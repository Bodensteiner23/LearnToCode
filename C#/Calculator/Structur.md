# Calculator

## 1.0 User Input
### 1.1 Number clicked
1. Check if Number is valid
2. Save the Number in a variable
3. Output Number into Label

### 1.2 Operator clicked
1. Check if Operator is valid
2. Readout Number in the Label and store it in _reservedNumber1
3. Add Operator "+" and userNumber to the Label Output
    #### 1.2.1 New User Input
    1. Readout Number in the Label and store Input in _reservedNumber2